# Sample Norwegian Raw Lesson

This folder shows the expected Norwegian raw text format.

It intentionally does not include `audio.wav`. Add a real recording named `audio.wav` before running ingestion for this sample.

Required files for a working lesson:

- `original.no.txt`
- `annotated.no.txt`
- `audio.wav`
- optional `meta.json`

